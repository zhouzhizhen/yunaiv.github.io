<section id="about">

    <% if (config.description) { %>
    <%- markdown(config.description) %>
    <% } %>

    <img src="http://www.iocoder.cn/images/common/wechat_mp_2017_07_31.jpg" alt="" class="ui centered image">

    <blockquote>
        <p>🙂🙂🙂 关注<strong>微信公众号：【芋道源码】</strong>有福利：  </p>
        <ol class="ui list">
            <li>RocketMQ / MyCAT / Sharding-JDBC <strong>中文注释源码 GitHub 地址</strong>  </li>
            <li>您对于源码的疑问每条留言<strong>都</strong>将得到<strong>认真</strong>回复。<strong>甚至不知道如何读源码也可以请教噢</strong>。  </li>
            <li><strong>新的</strong>源码解析文章<strong>实时</strong>收到通知。<strong>每周更新一篇左右</strong>。</li>
        </ol>
    </blockquote>

</section>

<div id="archive">
  <ul class="post-list">
    <% var year = 0 %>
    <% var change = false %>
    <% page.posts.sort('date', 'asc').each(function(post) { %>
      <% var itemYear = date(post.date, 'YYYY') - 0 %>
      <% change = year !== itemYear %>
      <% year = change ? itemYear : year %>
      <% if (change) { %>
        <h2><%= year %></h2>
      <% } %>
      <li class="post-item">
        <%- partial('_partial/post/date', { post: post, class_name: 'meta' }) %>
        <span><%- partial('_partial/post/title', { post: post, index: true, class_name: '' }) %><span>
      </li>
    <% }); %>
  </ul>
  <%- partial('_partial/pagination') %>
</div>
