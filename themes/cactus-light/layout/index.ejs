<section id="about">
  <% if (config.description) { %>
    <%- markdown(config.description) %>
  <% } %>

  <img src="http://www.yunai.me/images/common/wechat_mp ori.jpeg" alt="" class="ui centered image">

  <blockquote>
    <p>🙂🙂🙂 关注<strong>微信公众号：【芋艿的后端小屋】</strong>有福利：  </p>
      <ol class="ui list">
          <li>RocketMQ / MyCAT / Sharding-JDBC <strong>中文注释源码 GitHub 地址</strong>  </li>
          <li>您对于源码的疑问每条留言<strong>都</strong>将得到<strong>认真</strong>回复。<strong>甚至不知道如何读源码也可以请教噢</strong>。  </li>
          <li><strong>新的</strong>源码解析文章<strong>实时</strong>收到通知。<strong>每周更新一篇左右</strong>。</li>
      </ol>
  </blockquote>
</section>

<section id="writing">
    <span class="h1">Category</span>
    <ul class="post-list">
        <% site.categories.sort('name').each(function(item){ %>
            <li class="post-item">
                <div class="meta">
                </div>
                <span>
                    <a href="categories/<%= item.name %>"><%= item.name %><span>（<%= item.posts.length %>）</span></a>
                </span>
            </li>
        <% }); %>
    </ul>
</section>

<section id="writing">
  <span class="h1"><a href="<%- url_for("archives") %>">Writing</a></span>
  <ul class="post-list">
    <% if (theme.customize.show_all_posts) { %>
      <% var show_posts = page.posts.sort('date', 'desc') %>
    <% } else { %>
      <% var show_posts = site.posts.sort('date', 'desc').limit(theme.customize.post_count || 5) %>
    <% } %>
    <% show_posts.each(function(post, i){ %>
      <li class="post-item">
        <%- partial('_partial/post/date', { post: post, class_name: 'meta' }) %>
        <span><%- partial('_partial/post/title', { post: post, index: true, class_name: '' }) %></span>
      </li>
    <% }); %>
  </ul>
  <% if (theme.customize.show_all_posts) { %>
    <%- partial('_partial/pagination') %>
  <% } %>
</section>



<% if (site.data.projects) { %>
<section id="projects">
  <span class="h1"><a href="<%- url_for(theme.projects_url) %>">Projects</a></span>
  <ul class="project-list">
    <% for(var obj in site.data.projects){ %>
      <li class="project-item">
        <a href="<%= site.data.projects[obj].url %>"><%= site.data.projects[obj].name %></a>: <%= site.data.projects[obj].desc %>
      </li>
    <% } %>
  </ul>
</section>
<% } %>